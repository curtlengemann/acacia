package main.java.com.acacia.runtime.values;

/**
 * Different types of runtime values that can be used.
 */
public enum ValueType {
    NULL,
    NUMBER,
    STRING,
    OBJECT,
    BOOLEAN,
    NATIVE_FUNCTION,
    FUNCTION,
}
